default: &default
  host: localhost
  port: 3306
  user: root
  password: root
  database: orm_mysql

dev:
  <<: *default

prod:
  <<: *default
  port: <%= ENV["MYSQL_PORT"] || "3306"%>
  host: <%= ENV["MYSQL_HOST"] || "localhost"%>
  user: <%= ENV["MYSQL_USER"] || "root"%>
  password: <%= ENV["MYSQL_PASS"] || "root" %>
  database: prod_orm_mysql